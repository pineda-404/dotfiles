return {
  {
    'lazy.nvim',
    name = 'minimal-theme-loader', -- A name makes it easier to identify
    enabled = false,
    init = function()
      -- vim.cmd.colorscheme 'minimal'
    end,
  },
}
